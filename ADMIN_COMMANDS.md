# 🔐 Админ команды бота

## Доступные команды

### `/admin` - Админ-панель
Основная команда для доступа к админ-панели.

**Функции:**
- 👤 Изменить ник поддержки
- 💳 Изменить номер карты
- 👨 Изменить имя держателя карты
- 🏦 Изменить название банка

**Использование:**
```
/admin
```

### `/admin1236` - Альтернативная админ-панель
Альтернативная команда для доступа к той же админ-панели.

**Функции:** (идентичны `/admin`)
- 👤 Изменить ник поддержки
- 💳 Изменить номер карты
- 👨 Изменить имя держателя карты
- 🏦 Изменить название банка

**Использование:**
```
/admin1236
```

## Настройки, которые можно изменить

### 1. Ник поддержки
Telegram username для техподдержки (без @)

**Пример:**
```
support_bot
```

**Где используется:**
- Команда `/support`
- Кнопка "Написать в поддержку"

### 2. Номер карты
Номер банковской карты для пополнения баланса

**Формат:**
```
1234 5678 9012 3456
```

**Где используется:**
- Окно пополнения баланса в приложении
- Инструкции для пользователей

### 3. Имя держателя карты
ФИО владельца карты (латиницей, как на карте)

**Формат:**
```
IVAN IVANOV
```

**Где используется:**
- Окно пополнения баланса
- Инструкции для перевода

### 4. Название банка
Название банка-эмитента карты

**Примеры:**
```
Sberbank
Tinkoff
Alfa-Bank
```

**Где используется:**
- Окно пополнения баланса
- Информация для пользователей

## Доступ

### Кто может использовать?
**Все пользователи** имеют доступ к админ-панели.

Это сделано для удобства, так как:
- Каждый пользователь является воркером для своих рефералов
- Настройки карты нужны для приема платежей от рефералов
- Нет критичных функций, которые могут навредить системе

### Если нужно ограничить доступ

Отредактируйте функцию `admin()` в `bot.py`:

```python
async def admin(update: Update, context: ContextTypes.DEFAULT_TYPE):
    """Обработчик команды /admin - админ-панель"""
    user_id = update.effective_user.id
    
    # Список разрешенных админов
    ALLOWED_ADMINS = [123456789, 987654321]  # Замените на реальные ID
    
    if user_id not in ALLOWED_ADMINS:
        await update.message.reply_text(
            "❌ У вас нет доступа к админ-панели.\n\n"
            "Обратитесь к администратору."
        )
        return
    
    # Остальной код...
```

## Безопасность

### Что хранится в базе данных

Все настройки сохраняются в таблице `system_settings`:

```sql
SELECT * FROM system_settings;
```

| setting_key | setting_value | updated_at | updated_by |
|------------|---------------|------------|------------|
| support_username | support_bot | 2024-12-19 | 123456789 |
| card_number | 1234 5678 9012 3456 | 2024-12-19 | 123456789 |
| card_holder | IVAN IVANOV | 2024-12-19 | 123456789 |
| card_bank | Sberbank | 2024-12-19 | 123456789 |

### Логирование

Каждое изменение настроек записывается с:
- `updated_at` - время изменения
- `updated_by` - ID пользователя, который изменил

### Рекомендации

1. **Не используйте личные карты** - заведите отдельную карту для бизнеса
2. **Регулярно проверяйте настройки** - убедитесь что данные актуальны
3. **Храните резервные копии** - экспортируйте настройки из БД
4. **Ограничьте доступ** - если нужно, добавьте проверку админов

## Примеры использования

### Изменение ника поддержки

1. Отправьте `/admin` или `/admin1236`
2. Нажмите "👤 Изменить ник поддержки"
3. Введите новый username (без @): `new_support`
4. Готово! ✅

### Изменение номера карты

1. Отправьте `/admin` или `/admin1236`
2. Нажмите "💳 Изменить номер карты"
3. Введите номер: `1234 5678 9012 3456`
4. Готово! ✅

### Отмена операции

В любой момент можно отправить `/cancel` чтобы отменить текущую операцию.

## Связанные команды

- `/worker` - Панель воркера (управление рефералами)
- `/support` - Техподдержка
- `/start` - Главное меню

## Технические детали

### Файлы

- `bot.py` - обработчики команд
- `database.py` - функции работы с настройками
- `config.py` - конфигурация бота

### Функции в database.py

```python
# Получить настройку
await get_setting('support_username')

# Обновить настройку
await update_setting('card_number', '1234 5678 9012 3456', user_id)

# Получить все настройки
await get_all_settings()
```

### ConversationHandler

Команда использует `ConversationHandler` для пошагового ввода данных:

```python
WAITING_FOR_SUPPORT_USERNAME = 2
WAITING_FOR_CARD_NUMBER = 3
WAITING_FOR_CARD_HOLDER = 4
WAITING_FOR_CARD_BANK = 5
```

---

**Версия:** 2.0  
**Дата:** 19.12.2024  
**Статус:** ✅ Работает
